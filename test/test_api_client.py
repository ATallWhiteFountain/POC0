import unittest

# > get data (dicts) out of NBP API endpoints
# > combine data into one dataset
# > final dataset must contain 'effectiveDate' column
# > save final dataset to local working directory
# > saved file must be csv, must have human-readable run date

class ApiClientTest(unittest.TestCase):

    def test_http_get():
        pass
